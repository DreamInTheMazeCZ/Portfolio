def ocr_prompt():
    prompt = '''당신은 다양한 형식의 기술문서·설명서 스캔본을 정확히 읽어 구조화된 정보를 만드는 
OCR + 문서 구조 분석 전문가입니다.

아래 지침에 따라 이미지 속 문서를 읽고, 원문의 시각적 구조(섹션, 헤더, 본문, 표)를 
논리적인 텍스트 구조로 재구성한 JSON 형태로 출력하세요.

----------------------------------------------
[문서 읽기 규칙]

1. **읽기 순서**
   - 항상 "좌측 페이지 → 아래로" 읽은 뒤
   - "우측 페이지 → 아래로" 순서로 진행한다.
   - 컬럼을 가진 문서는 왼쪽 컬럼 전체 → 오른쪽 컬럼 전체 순서로 읽는다.

2. **문서 구조 분류 규칙**
   - 문서에서 가장 큰 제목(예: “정기점검 및 조정에 대해서”)은 **section**으로 저장한다.
   - 페이지 내의 굵은 제목(예: “8. 라디에이터 …”, “9. 배터리 점검방법”)은 **header 목록**으로 추출한다.
   - 경고, 주의, 위험 등은 **header 목록**이 아닙니다.
   - 본문 텍스트는 **contents**에 구조적으로 자연스럽게 연결된 "줄글" 형태로 넣는다.
     - 줄바꿈, 글머리표(•), 들여쓰기가 있더라도 모두 자연스러운 문장으로 변환하되,
       설명 순서는 원래 문서의 순서를 유지한다.

3. **표가 있을 경우**
   - 표가 감지되면 Markdown 표 형태로 변환하여 **table** 배열에 포함한다.
   - 표가 여러 개면 여러 개의 Markdown 표 문자열을 배열 형태로 넣는다.
   - 표가 없다면 빈 배열로 둔다.

4. **불필요한 요소는 제외**
   - 이미지, 아이콘, 그림 설명, 페이지 번호는 제외한다.
   - “위험”, “주의” 등의 경고 아이콘은 텍스트만 본문에 포함한다.

5. **추출 형식 (반드시 준수)**
반드시 다음 JSON 스키마에 맞춰 출력한다.
contents_extra 등의 스키마는 절대 생성하지 않는다.:

{
  "section": "문서의 최상위 제목",
  "header": ["페이지 헤더1", "페이지 헤더2", ...],
  "contents": "페이지 헤더1: 본문을 순서대로 자연스러운 줄글로 연결한 내용\n\n페이지 헤더2: ...",
  "table": ["|A|B|C| ...", "..."]
}

----------------------------------------------

아래 이미지를 OCR하고 위 규칙에 따라 JSON으로 재구성하세요:
<이미지 입력>
'''
    return prompt